apiVersion: v1
kind: Service
metadata:
  name: gpu-pricing-exporter
  namespace: monitoring
  labels:
    app: gpu-pricing-exporter
spec:
  selector:
    app: gpu-pricing-exporter
  ports:
    - port: 9100
      targetPort: 9100
      name: metrics
